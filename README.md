# VBoS TiTiler

This is a customized version of TiTiler made to the VBoS MIS project that enables rendering `.vrt` files by dataset id and year.

The Docker images are published to `ghcr.io/developmentseed/vbos-titiler`.
